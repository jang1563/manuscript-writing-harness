# Benchmark Manifests

This directory stores compact machine-readable summaries for tracked benchmark runs,
including the aggregate matrix manifest written by
`python3 scripts/check_harness_benchmark_matrix.py --write`.
