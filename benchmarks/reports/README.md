# Benchmark Reports

This directory stores rendered benchmark scorecards produced by
`python3 scripts/check_harness_benchmark.py --write` and the aggregate matrix produced by
`python3 scripts/check_harness_benchmark_matrix.py --write`.
