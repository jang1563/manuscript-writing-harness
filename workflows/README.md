# Workflows

This directory holds workflow metadata that should remain version-controlled and human-readable.

Current contents:

- agent architecture metadata
- venue configs
- venue checklists
- release notes scaffolding
- release profiles and integrated release-bundle metadata
- archive and export assembly metadata
