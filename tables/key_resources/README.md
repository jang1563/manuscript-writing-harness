# Key Resources Table

This directory stores the tracked overlay skeleton for a Cell-style Key Resources Table.

The current files are structured placeholders so venue readiness checks can resolve to explicit paths before a project-specific resource inventory is finalized.
