# Table Source

Place table generation scripts here.

Expected future outputs:

- manuscript-ready tables
- supplementary tables
- CSV and spreadsheet exports for collaborators

Current example:

- `build_main_table.py`: validates `tables/data/example_main_table.csv` against `tables/schemas/main_table_schema.yml` and emits markdown, CSV, JSON, and manifest outputs

