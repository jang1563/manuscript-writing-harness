# Reference Manifests

Generated package manifests for bibliography and citation-graph handoff live here.

Current primary artifact:

- `reference_package.json`

Regenerate with:

`python3 scripts/check_reference_integrity.py --write --sync-graph`
