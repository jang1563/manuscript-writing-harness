# Figure Specs

Store renderer-agnostic figure specifications here.

Each spec should define:

- figure identity and title
- venue profile
- size in millimeters
- shared data inputs
- expected panel-level source-data files
- claim identifiers
- fact-sheet and visualization-plan links
- enabled renderers

The spec is the shared contract between the Python and R figure pipelines.
