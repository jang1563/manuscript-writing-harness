# Generated Assets

This directory holds generated preview assets copied into the manuscript project so MyST can render them reliably.

The sync step also mirrors required preview files under `../sections/assets/generated/` because MyST builds section pages independently as well as inside the top-level manuscript.

These files are derived artifacts, not the authoritative figure source.

The authoritative figure assets remain under:

- `../figures/output/`
