# Section Preview Assets

This directory mirrors generated preview assets for section-level MyST builds.

The canonical synced preview assets still live under `../../../assets/generated/`.

The duplication here is intentional so include-based display items resolve both:

- from the top-level manuscript page
- from standalone section pages such as `sections/03_results.md`
