# Local Template Overrides

Use this directory only when a journal overlay requires local customization beyond upstream MyST or jtex templates.

Preference order:

1. use a maintained upstream template
2. add venue config values
3. add local overrides here only when necessary

