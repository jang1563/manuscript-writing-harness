### Bundle claim 1. Model discrimination should stay visible under both ROC and precision-recall evaluation

Use `figure_06_roc_pr_compound` to draft the final Results prose for this bundle slot.

```{include} ../figure_06_roc_pr_compound.md.txt
```

### Bundle claim 2. Probability quality should be evaluated with calibration and support together

Use `figure_07_calibration_reliability` to draft the final Results prose for this bundle slot.

```{include} ../figure_07_calibration_reliability.md.txt
```

### Bundle claim 3. Optimization quality should be shown as trajectories rather than endpoint snapshots

Use `figure_08_training_dynamics` to draft the final Results prose for this bundle slot.

```{include} ../figure_08_training_dynamics.md.txt
```

### Bundle claim 4. Error analysis should show localized failure pathways instead of only aggregate metrics

Use `figure_09_confusion_matrix_normalized` to draft the final Results prose for this bundle slot.

```{include} ../figure_09_confusion_matrix_normalized.md.txt
```

### Bundle claim 5. Interpretation should emphasize biologically meaningful signals and effect direction

Use `figure_10_feature_importance_summary` to draft the final Results prose for this bundle slot.

```{include} ../figure_10_feature_importance_summary.md.txt
```

### Bundle claim 6. Ablation studies should separate top-line performance effects from calibration effects

Use `figure_11_ablation_summary` to draft the final Results prose for this bundle slot.

```{include} ../figure_11_ablation_summary.md.txt
```

### Bundle claim 7. Representation geometry should preserve state structure without hiding domain support

Use `figure_12_embedding_projection` to draft the final Results prose for this bundle slot.

```{include} ../figure_12_embedding_projection.md.txt
```

### Bundle claim 8. Abstention curves should pair risk reduction with retained coverage

Use `figure_13_uncertainty_abstention_curve` to draft the final Results prose for this bundle slot.

```{include} ../figure_13_uncertainty_abstention_curve.md.txt
```
