# Bundle Display Items

Managed bundle include files live here.

Do not edit these files directly. Refresh them with:

- `python3 scripts/figures_cli.py apply-bundles --all`

Each file mirrors the tracked `results_fragment.md` inside the corresponding bundle directory.
